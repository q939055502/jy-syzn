## 检测系统重构完成计划

### 一、问题分析

在检查路由和服务目录后，发现以下问题需要修复：

1. **路由初始化文件**：`app/routes/detection/__init__.py` 中仍然引用了 `groups_router`，导致路由冲突
2. **旧服务文件**：`app/services/detection/detection_group_service.py` 文件仍然存在，使用了旧的 `DetectionGroup` 模型
3. **路由文件命名**：`app/routes/detection/groups.py` 文件名与内容不符，其内容已修改为处理检测项目

### 二、解决方案

1. **更新路由初始化文件**：
   - 删除 `app/routes/detection/__init__.py` 中对 `groups_router` 的导入和注册

2. **删除旧服务文件**：
   - 删除 `app/services/detection/detection_group_service.py` 文件

3. **确保路由一致性**：
   - 确认 `app/routes/detection/items.py` 已正确实现检测参数相关功能
   - 确认 `app/routes/detection/groups.py` 已正确实现检测项目相关功能

### 三、验证步骤

1. 运行测试脚本验证重构效果
2. 检查API文档是否需要更新
3. 检查前端修改说明是否需要更新

### 四、预期结果

- 所有对 "group" 的引用已被替换为 "item"
- 所有对 "item" 的引用已被替换为 "param"
- 路由配置正确，没有冲突
- 测试脚本执行成功
- 文档已更新，与实际代码一致