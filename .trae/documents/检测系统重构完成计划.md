# 检测系统重构完成计划

## 一、文件清理与重命名

1. **删除旧文件**：
   - `detection_group.py` - 旧的检测项目组模型
   - `detection_item.py` - 旧的检测项目模型

2. **重命名新文件**：
   - 将 `detection_item_new.py` 重命名为 `detection_item.py`

## 二、命名规范检查

1. **模型命名**：
   - 确保所有模型类名、属性名、方法名符合其实际意义
   - 检查关联表命名是否一致

2. **服务命名**：
   - 确保所有服务类名、方法名符合其实际意义
   - 检查服务间调用是否使用正确的命名

3. **路由命名**：
   - 确保所有路由名称、端点名称符合其实际意义
   - 检查请求和响应模型命名是否一致

## 三、测试验证

1. **运行测试脚本**：
   - 使用 `test_detection_refactor.py` 验证所有检测相关端点
   - 确保所有测试通过

## 四、文档更新

1. **API 文档**：
   - 更新 `API_DOCUMENTATION.md`，确保所有检测相关端点的命名正确
   - 检查请求和响应示例中的命名是否一致

2. **模型说明文档**：
   - 更新 `app/models/detection/模型说明.md`，确保模型命名和关系正确
   - 检查表关系图中的命名是否一致

3. **前端修改说明**：
   - 更新 `前端修改说明.md`，确保所有命名变更正确反映
   - 检查 API 端点、数据结构、请求参数的命名是否一致

## 五、具体执行步骤

1. 删除旧文件 `detection_group.py` 和 `detection_item.py`
2. 将 `detection_item_new.py` 重命名为 `detection_item.py`
3. 检查所有模型、服务、路由的命名规范
4. 运行测试脚本验证重构效果
5. 更新 `API_DOCUMENTATION.md`
6. 更新 `app/models/detection/模型说明.md`
7. 更新 `前端修改说明.md`

## 六、预期结果

- 所有旧文件被删除
- 新文件命名正确
- 所有命名符合其实际意义
- 测试脚本通过
- 所有文档更新完成，命名一致