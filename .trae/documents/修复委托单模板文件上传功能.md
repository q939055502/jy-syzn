## 修复方案

### 1. 修复文件路径问题
**问题**：`get_absolute_file_path` 函数计算错误，多包含了 `app` 目录
**修复**：修改 `file_utils.py` 中的 `get_absolute_file_path` 函数，向上走5级目录而不是4级

### 2. 完善测试脚本
**添加测试用例**：
- 验证文件是否被正确保存到预期位置
- 验证改变检测项目时文件是否被正确移动
- 验证更新版本号时文件名是否被正确修改
- 验证上传新文件时旧文件是否被删除，新文件是否被正确保存

### 3. 执行测试
- 运行修复后的测试脚本
- 手动检查文件系统，确认文件被正确保存和移动

## 修复步骤

1. **修改 `file_utils.py`**：修正 `get_absolute_file_path` 函数
2. **更新测试脚本**：添加文件系统验证逻辑
3. **执行测试**：运行测试脚本，验证所有功能
4. **手动验证**：检查文件系统，确认文件保存和移动正确

## 预期结果

- 文件被正确保存到 `D:\Projects\jy_syzn\static\templates\水泥检测\` 目录
- 改变检测项目时，文件被移动到对应目录
- 更新版本号时，文件名被正确修改
- 上传新文件时，旧文件被删除，新文件保存到对应目录