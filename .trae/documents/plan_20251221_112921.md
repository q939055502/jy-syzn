## 修改认证接口返回用户信息计划

### 1. 修改登录接口 (`login_for_access_token` 函数)
- **位置**：`app/routes/auth.py` 第84-96行
- **修改内容**：在返回的用户信息中添加以下字段：
  - `is_admin`：用户是否为管理员
  - `created_at`：用户创建时间（ISO格式）
  - `updated_at`：用户信息更新时间（ISO格式）
  - `last_login_at`：用户最后登录时间（ISO格式）

### 2. 修改获取当前用户信息接口 (`get_current_user_info` 函数)
- **位置**：`app/routes/auth.py` 第187-194行
- **修改内容**：在返回的用户信息中添加与登录接口相同的字段

### 3. 确保日期时间格式统一
- 使用 ISO 8601 格式返回日期时间字段
- 处理可能为 None 的日期时间字段

### 4. 更新API文档
- **位置**：`API_DOCUMENTATION.md`
- **修改内容**：更新登录接口和获取当前用户信息接口的响应示例，添加新字段

### 5. 验证修改结果
- 运行测试脚本验证修改后的功能
- 确保所有接口响应格式符合预期