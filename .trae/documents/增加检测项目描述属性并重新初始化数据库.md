1. **修改检测项目模型**：在 `detection_item.py` 中添加描述属性
2. **更新业务逻辑**：确保所有相关业务逻辑（创建、更新、查询等）都支持描述属性
3. **修改初始化脚本**：在 `init_all.py` 中为检测项目添加描述字段
4. **重新初始化数据库**：运行初始化脚本删除现有数据、创建新表并初始化数据

### 具体实施步骤

#### 1. 修改检测项目模型
- 在 `DetectionItem` 类中添加 `description` 字段
- 字段类型：`String(255)`，允许为空
- 添加适当的注释

#### 2. 更新业务逻辑
- 检查 `DetectionItemService` 中的所有方法，确保支持描述属性
- 检查 API 路由，确保描述属性在请求和响应中得到正确处理
- 检查 DTO 模型，确保描述属性被包含

#### 3. 修改初始化脚本
- 在 `init_all.py` 的 `detection_items_to_create` 列表中为每个检测项目添加 `description` 字段

#### 4. 重新初始化数据库
- 停止当前运行的应用
- 运行 `python script/init_all.py` 重新初始化数据库
- 验证检测项目表中是否包含描述字段
- 验证初始化数据是否包含描述信息

### 预期结果
- 检测项目表中新增 `description` 字段
- 所有业务逻辑支持描述属性
- 初始化数据包含检测项目描述
- 数据库表结构正确，数据初始化成功