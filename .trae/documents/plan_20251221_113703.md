## 重构认证服务，分离业务逻辑到服务层

### 1. 在服务层添加业务逻辑方法
在 `auth_service.py` 中添加 `get_user_with_permissions` 静态方法，将接口层中重复的业务逻辑（获取用户角色和权限信息）迁移到服务层。

### 2. 修改接口层代码
修改 `auth.py` 中的两个接口：
- `login_for_access_token`：使用服务层新方法获取用户完整信息
- `get_current_user_info`：使用服务层新方法获取用户完整信息

### 3. 验证修改结果
运行测试脚本验证修改后的功能是否正常工作，确保所有接口响应格式符合预期。

### 实施步骤
1. 修改 `auth_service.py`，添加 `get_user_with_permissions` 方法
2. 修改 `auth.py` 中的 `login_for_access_token` 函数
3. 修改 `auth.py` 中的 `get_current_user_info` 函数
4. 运行测试脚本验证修改结果