## 修复方案

### 1. 更新API接口说明文档

**修改内容**：
1. **文件类型限制**：明确说明只允许.doc、.docx、.xls、.xlsx格式
2. **文件路径格式**：更新为`/static/templates/{检测项目名称}/{模板名称}{模板版本}.{文件类型}`
3. **响应示例**：更新所有示例中的文件类型和文件路径
4. **接口描述**：添加关于文件操作的说明

**具体修改点**：

#### 3.6.1 获取委托单模板列表
- 更新响应示例中的文件类型和文件路径

#### 3.6.2 获取单个委托单模板
- 更新响应示例中的文件类型和文件路径

#### 3.6.3 创建委托单模板
- 在请求参数说明中添加文件类型限制
- 更新请求示例和响应示例中的文件类型和文件路径

#### 3.6.4 更新委托单模板
- 在请求参数说明中添加文件类型限制
- 更新响应示例中的文件类型和文件路径

#### 3.6.5 删除委托单模板
- 无需修改

### 2. 执行修改

使用Edit工具修改API接口说明文档，确保所有委托单模板相关的接口都符合修复后的业务逻辑。

### 3. 验证修改

检查修改后的API接口说明文档，确保所有修改点都已正确更新，并且文档内容与实际业务逻辑一致。